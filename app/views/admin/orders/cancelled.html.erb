<h1>Cancelled Orders</h1>


<% @orders.each do |order| %>
  <p><%= link_to "Order #{order.id}", order_path(order) %> was cancelled on <%= order.updated_at.strftime("%A, %d %b %Y %l:%M %p") %></p>
<% end %>
